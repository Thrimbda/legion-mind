# Legion Maintenance

## Open follow-ups

- `publish-lgmind-npm` 已完成首次手动 npm publish：`lgmind@0.1.0`，`latest` -> `0.1.0`。后续应新增独立任务，为未来 npm releases 配置 GitHub CI trusted publishing，减少手动 OTP 发布路径。
- `harden-strict-verify-integrity` 留下一个非阻塞审计增强点：未来若需要更强 manifest 自一致性，可要求 `managed-files.v1.json` 中 `record.targetPath === manifest key`；当前实现不让该字段驱动路径访问，因此不影响本次安全边界。
- `build-vibeharnessbench-mvp` 的“升级 contract verifier”事项已由 `complete-vibeharnessbench-v01` 在 local-first semantic scope 内完成；剩余高保真边界见下一条。
- `complete-vibeharnessbench-v01` 仍未覆盖 Docker-faithful full stack、binary GIF pHash/SSIM、real RPC process harness、browser/ffmpeg/Playwright full-stack checks；这些是 RFC 非阻塞边界，若需要应拆独立任务。
- `complete-vibeharnessbench-v01` 当前 local subprocess isolation 不等同于可安全运行任意恶意 HUT 的 sandbox/container/chroot；如要评估不可信 HUT，应新增 sandbox/container 执行器任务。
- `complete-vibeharnessbench-v01` 可追加 starter/public symlink hardening，拒绝指向 case root、protected dirs 或 repo 外敏感路径的 symlink。
- `harden-v1-kernel-harness` 留下一个非阻塞可读性增强点：未来可小幅重构 `scripts/lib/setup-core.ts` 中 managed root textual/canonical 对应关系，让 `targetWithinManagedRoots` 的 symlink-root 拒绝逻辑更容易审计；当前实现对已验证的破坏性 rollback/uninstall case 保持保守，并已有 regression 覆盖。
- `extract-linear-scheduler-npm-project` 留下一个非阻塞 CI 可见性增强点：如果 `scheduler/` 成为长期维护的运行时项目，应新增 CI job 或现有 workflow step 显式运行 `npm --prefix scheduler test`，保持 root regression 与 scheduler regression 的分离可见。
- `linear-legion-scheduler-wi-03` 未在真实 Linear test project 上执行 `scan project`，因为当前环境没有 repo-local `LINEAR_API_KEY` / 专用测试 project。启用真实调度前应使用只读 / 最小权限 token 对测试 project 跑一次 dry-run，并人工核对 3-5 个 issue 的 ready/skipped/cycle 输出。
- `linear-0xc-58` 未对真实 OpenCode worker / Linear Native Agent API 执行 live smoke；当前使用 fake OpenCode / fake native adapter 覆盖本地 contract。启用真实调度前应使用测试 WI、最小权限 native app / token、repo-local prompt/log artifacts 跑一次 dry-run，并核对 AgentSession / delegate / activity / stop behavior。
- `linear-0xc-58` 的 non-blocking hardening：未来可把 evidence gate 使用的 `risk` / `contractState` 从 evaluated snapshot 持久化到 run，而不是从 dispatch payload 读取；dispatch identity rejection 可转成显式 blocked/failed run state；worker stdout/stderr log artifact 可加 owner-only mode、redaction 或 retention policy。
- `linear-0xc-60` 未对真实 GitHub API / Linear Native Agent API 执行 live smoke；当前使用 PR fixture、static client 与 repo-local evidence fixtures 覆盖本地 delivery contract。启用真实调度前应使用 least-privilege GitHub token 对测试 PR 跑一次 `delivery track`，并用生产 Linear native adapter 核对 external URL、activity、state/labels、final comment / response 幂等写回。
- `linear-0xc-60` 的 non-blocking hardening：未来可把 GitHub required checks / branch protection / required review policy 纳入 adapter snapshot，避免只凭 check-runs + review list 推断“checks/review resolved”；如果 scheduler DB 成为长期运行 DB，应补 legacy SQLite migration test 覆盖旧 `native_outbox.side_effect` CHECK constraint。
- `linear-0xc-61` 未对真实 Linear project / production native adapter 执行 live parallel dispatch；当前使用 fixture scanner/dispatcher 覆盖本地 contract。启用自动并行调度前应在测试 project 上跑 `dispatch fixture` 等价 live dry-run，确认 lock/capacity/blocker wait 在 Linear UI 中可见且不会写 `agent:running`。
- `linear-0xc-61` 的 non-blocking hardening：WI-07 / WI-08 应补 admin stale recovery / release command、repo parallelism 的 richer config，以及生产 native adapter 对 waiting visibility payload 的幂等消费。
- `linear-0xc-62-webhooks-retry-recovery` 未对真实 Linear webhook / native app 执行 live smoke；当前使用 raw-body HMAC fixtures、fake AgentSessionEvent 和 local store 覆盖 contract。启用生产 webhook 前应使用测试 webhook secret / native app，在测试 project 上验证 signature、timestamp、duplicate delivery、created/prompted/stopped/delegated/permission-change routing 与 5s ack / 10s activity expectation。
- `linear-0xc-62-webhooks-retry-recovery` 的 non-blocking hardening：未来 production scheduler outbox worker 应消费 `reconcile_project` / `native_session_event` / `permission_change` side effects；长期运行时可把 retry `notBefore` metadata 升级成 durable delayed queue，并补 metrics/admin UX。
- `linear-0xc-59-operations-security` 未对真实 Linear OAuth app / GitHub token / production native adapter 执行 live security smoke；当前使用 local fixtures 覆盖 admin CLI、redaction、PermissionChange 和 project controls。启用生产调度前应在测试 project 上验证 app actor scopes、delegate/mention scopes、PermissionChange revocation、GitHub least-privilege token 与 `project pause/resume` 操作 drill。
- `linear-0xc-59-operations-security` 的 non-blocking hardening：长期运行时可增加 Prometheus/OpenTelemetry exporter、project-id denormalized scheduler events for cleaner health filtering、生产 native adapter consumption of security/metrics events，以及 log retention / redacted artifact cleanup policy enforcement。
- `accept-linear-scheduler-overall` 总体验收结论：`scheduler/` 本地原型通过（57/57 tests + health/scan/dispatch/delivery/project-health CLI smoke），但生产上线仍 blocked。生产前必须完成真实 Linear read-only scan、sandbox Linear native writeback、live GitHub PR tracker、real OpenCode worker E2E、security/observability/retention 和 staged rollout。
- `accept-linear-scheduler-overall` 记录的部分文档漂移已由 `prepare-linear-scheduler-production-acceptance` 处理：新增 `scheduler/tests/fixtures/project.json`，修正 README fixture paths，并把 `linear-admin-observability.test.ts` 加入 README 测试清单。仍开放的生产化清理项：WI-04..WI-08 work-item acceptance checklist 未与 delivered artifact 回填；manual Linear Done audit 需要在 reconcile/scan-to-dispatch 边界保证；native stop/cancel lock release 生产化前应等 worker side effects 停止或 cleanup 完成；production Linear state adapter 必须使用配置映射，不得把 `suggestedState` 当硬编码状态名。
- `prepare-linear-scheduler-production-acceptance` 留下的预期 blockers：production Linear native writeback adapter、live `dispatch project` command、packaged webhook server/outbox runner、external metrics/exporter/retention policy 和 real OpenCode E2E 仍未实现或未验证；生产验收时应按 runbook 显式记录为 BLOCKED，而不是用 fixture/local results 替代。
- 若后续要求“全仓历史英文设计文档中文化”，应另开专门迁移任务；本次 `localize-production-acceptance-docs` 只修正 PR #44 生产验收准备包及直接相关入口/证据，避免在紧急语言修正中重写历史 RFC / WI 文档。
