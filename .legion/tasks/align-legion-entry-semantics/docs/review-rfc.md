# RFC 审查报告

## 结论
PASS

## 阻塞问题
- [ ] 无

## 非阻塞建议
- `安全考虑` 中“`init` 仅创建最小骨架”这句话仍略显模糊，建议直接改成“`init` 仅保证 `.legion/tasks/` 存在”，避免再次让读者联想到 wiki skeleton。
- 可补一条冲突规则：当 `taskId` 与 `taskRoot` 同时给出但不匹配时直接报错，不做猜测。这样比“唯一映射”更可操作。
- 若 README 面向终端用户，建议尽量少用 `active task` 术语，优先写成“当前请求恢复的任务”，把旧词保留在 workflow 内部定义即可。

## 修复指导
1. 保持当前最小复杂度路线：**只收敛语义与文档，不扩展 `init` 实现。**
2. 文档落地时严格执行禁用措辞检查，确保 README / usage / skills / references 不再把 CLI 写成默认入口，也不再暗示隐式 current-task。
3. 顺手收紧剩余模糊表述，尤其是任何可能让人把“最小骨架”理解成 wiki skeleton 的句子。

## 可实现 / 可验证 / 可回滚检查
- **可实现**：通过。方案已收缩到文档与语义层，不依赖额外运行时改造。
- **可验证**：通过。RFC 已把验证收敛为术语检查、禁用措辞 grep 与 `init` 真实行为核对。
- **可回滚**：通过。当前方案主要是文档与 skill 文案收敛，回滚成本低，且不会回退到旧的持久化 current-task 语义。
