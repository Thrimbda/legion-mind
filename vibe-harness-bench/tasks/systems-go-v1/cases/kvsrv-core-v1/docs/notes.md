# Notes

This v0.1 task uses a clean-room Go simulator verifier. The verifier injects Go tests against the public root package `kvbench` and the documented versioned key/value API.

The starter is intentionally incomplete. Oracle and negative controls are protected and are not mounted into HUT workspaces.
